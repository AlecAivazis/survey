package probe

// get the current location of the cursor
// loc, err := CursorLocation()
// // if something went wrong
// if err != nil {
//  // yell loudly
//  panic(err)
// }
// start at the line after the cursor
// initialRow := loc.col + 1
// make sure we overwrite the line after the cursor
// tm.MoveCursor(initialRow, 1)

// for {
//  // make sure we overwrite the first line
//  tm.MoveCursor(initialRow, 1)

//  tm.Print("Current Time: ", time.Now().Format(time.RFC1123))

//  tm.Flush() // Call it every time at the end of rendering

//  time.Sleep(time.Second)
// }
